apiVersion: v1
kind: Service
metadata:
  name: {{ include "solar-creeper.fullname" . }}
  labels:
    {{- include "solar-creeper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "solar-creeper.selectorLabels" . | nindent 4 }}
