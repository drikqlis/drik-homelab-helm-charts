apiVersion: v1
kind: Service
metadata:
  name: {{ include "jagodanki.fullname" . }}
  labels:
    {{- include "jagodanki.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jagodanki.selectorLabels" . | nindent 4 }}
